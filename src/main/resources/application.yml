server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/paredesdaniel1721/config-data
          searchPaths: service #config-data
  security:
    user:
      name: root
      password: s3cr3t
