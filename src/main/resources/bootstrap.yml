server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/paredesdaniel1721/config-data.git
          searchPath: / #config-data
          #username: ${GIT_USER}
          #password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
